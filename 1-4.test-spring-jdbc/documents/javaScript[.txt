javascript 画面に動きを付ける

jsファイルに書いてbody内で相対パスで読み込む
読み込みのタイミングによってはhead内で書いたほうがいいパターンもある
画面描画が始まる前に処理が必要な時とか

データ型がないのでなんでも入れれる？
実装は楽だが想定しないデータ型が代入されて思わぬ挙動になったりする
var let const
基本はキャメルケース

厳密等価演算子でデータ型を比較できる
3 === 3 整数同士比較
3 === '3' 整数と文字列なのでfalse
3 !== 3 false
※ == もあるが値しか判定しないので 3 == '3' はtrueになる

配列は定義の仕方がjavaと若干異なる
let ages = [23, 46, 32];
console.log(ages[2]);

for(let age of ages){
console.log(age);
}


連想配列
キーとバリューをコロンでつなぐ
let curryInfo = {
food: 'カレー';
color: '赤';
spice: '激辛';
'感想' : '辛い';
};
let foodName = curryInfo['food'];
curryInfo['color'] = '緑';

